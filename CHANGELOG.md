# 版本记录

> 只记**对外可见的变化**（功能/结构/数据），每条都注明**怎么验证的**。
> 详细的每一步在 `git log`；改坏了用 `git checkout -- <文件>` 回滚单个文件。

---

## v1.2 · 录题台：扫描版试卷全自动入库（2026-09-16）

**功能**

- **`amti/record.py`** —— 扫描版试卷 PDF → exam-zh LaTeX。链路：
  `fitz 抽页图（原扫描图直取，不重编码）→ 本地 VLM 逐页识别 → 跨页合并 →
  答案/解析配对 → 带图题过滤 → 组装 LaTeX`。识别结果**一页一个 json**
  落在 `数据/录题/<卷名>/scan/`，中断重跑只补没做完的页。
- **`amti/web/record_server.py` + `record.html`** —— 独立录题台，端口 **8901**
  （不碰题库主服务的 8899）。拖 PDF 进浏览器就排队，界面上一眼看到
  「识别到几题 / 带图丢弃几题 / 8·11·14·18·19 题的位置登记 / 每题题干·答案·解析」，
  「干跑看看」确认后「确认入库」。
- **答案卷自动配对** —— 同目录下按文件名相似度配对（Z20+ 那种「试卷 + 答案」
  两个独立 PDF 的情况）。阈值 0.80 是拿 15 份真实卷名量出来的：真配对 1.00 / 0.86，
  最大的假配对 0.71，卡在 0.80 刚好只留真的。配错了界面上能改。
- **三条规矩写死在代码里**（不是提示词里的提醒）：带图题一律不录；
  只有第 8/11/14/18/19 题的带图题登记原卷位置；没有解析的写「解析无」。

**踩过的坑（都写进注释了）**

| 现象 | 根因 |
|---|---|
| 请求发出去**石沉大海**，客户端只能干等超时 | 一页扫描图 2266×3358 ≈ 7900 图像 token > 模型的 8192 上下文。压到 200 万像素（≈2100 token） |
| 并发 4 路，请求死掉只留 `CLOSE_WAIT` | LM Studio 的 8192 上下文**被 4 个槽平分**，每槽 2048，连一页图都装不下 → 默认改串行 |
| `finish_reason=length`，4000 token 全烧在思考上，正文为空 | `chat_template_kwargs.enable_thinking` / `thinking` / system 里加 `/no_think` **三种写法全被忽略**；只有 `reasoning_effort="none"` 真管用 |
| 批量跑到第 2 页 `AttributeError: 'int' object has no attribute 'get'` | 用页码覆盖了页信息字典。拆成 `no`（页码）/ `page`（kind + title）两个字段 |
| 并发时每页耗时张冠李戴 | 4 个线程共用一个 `LAST` dict，改成 `threading.local()` |

**验证**

- `python3 -m amti.record --selftest` 23 项用例（已挂进 `amti.py accept` 第①层）
- 提速：把模型按 `lms load --context-length 32768 --parallel 4` 重新加载后，
  4 路并发的 token 吞吐 ~21.6 t/s（串行 11.8 t/s），约 1.8 倍。

---

## v1.1 · 讲义系统完善（2026-09-15）

**功能**

- **生成进度动画** —— 点「生成 PDF」后画布上出现浮层：转圈 + 滑动进度条 +
  **阶段清单**（走过的 ✓、当前的 ●）+ 已用时间秒表。讲义 6 个阶段（整理内容 →
  生成页面 → 起 Chromium → 渲染 → 编译 → 收尾），组卷 4 个阶段（两遍 xelatex）。
  实现见 `web/src/app/CompileOverlay.tsx`。
- **高考题标出处** —— 讲义里高考题前加「（2024新高考I卷 第1题）」，**与题目同一行**；
  右栏开关「高考题标出处」（默认开），块级可单独覆盖。
  标签用纯文本（不用 `<span>`）—— 编辑器预览不解析 HTML，用标签会「预览有、导出没有」。
- **选项里的图片** —— 修掉「选项显示成 `(A)\includegraphics[...]{x.png}` 源码、
  且四个选项各占一行」。四个原因叠加：选项没走图片转换 / 图片包在 `$…$` 里 /
  Slidev 的 reset 让 `img` 是 block / 外层 flex 把选项压窄。现在选项图并排一行。

**稳定性**

- **导出子进程加 timeout（900s）+ 进程组强杀** —— 原来 Slidev 卡死会永久占住
  FastAPI 线程池的一个 worker，重复几次服务就"看着活着但所有接口排队"。
- **接口边界** —— `limit` 限制 1..1000（原来无上界，前端却在传 5000）；
  `_BRIEF` 缓存换成有上限的 LRU（2000）；导出端点串行化（忙则 429）。
- **日志** —— 新增 `amti/logutil.py`，全项目从 **0 处 logging** 变成 20+ 处，
  12 处裸 `except` 全部补上原因说明。`AMTIKU_LOG_LEVEL=DEBUG` 可调。
- **本地 API 防跨站** —— 加 `Origin` 白名单中间件（挡 CSRF / DNS-rebinding）。
- **列表瘦身** —— 列表只返回题干块 IR，答案/解析留给详情页
  （`parse_blocks` 一页少跑 2/3）；校验脚本用 `lite=0` 分页取全量。

**测试**

- `测试/接口测试.py`（14 条）+ `测试/讲义测试.py`（10 条），**不依赖 pytest** 也能跑。
- **`amti.py accept` 会自己按需起服务**（层③ 要连 HTTP 取全库）。
  原来验收不会起服务、没起就必失败，报的却是「✗ 未通过：端到端渲染」——
  而服务本身 25 秒空闲就自动退出，所以"别的都绿、只差层③"很容易踩到。
  现在：探一下，没跑就起一个临时的，跑完关掉（不留进程）。
- 两个真 bug 是测试抓出来的：`patch_question` 漏 `return`（接口 500）、
  `hmac.compare_digest` 比 str 遇中文口令崩。

**考点手动修正（用户反馈）**

- 详情页「考点」抽屉里：`×` **常显**（原来悬停才出现，找不到）、
  与主考点**不在同一章**的标签直接标出章名（例：数列题上的「1.1 集合的含义与表示」
  会标「集合与逻辑」，一眼看出可疑）、非主考点可一键**设为主**。
  `web/src/app/Detail.tsx` 的 `TagsEditor`。

**清理**

- 删了约 **420MB** 可再生成的缓存与中间产物（图片透明缓存 202MB、
  Slidev 的 `public/img` 183MB、自动备份、LaTeX 中间产物、`__pycache__`、`.DS_Store`）；
  `.gitignore` 补上 `*.aux/*.out/*.toc`、`试卷/*.log`。

---

## v1.0 · 讲义系统 + 结构重构 + git（2026-09-15）

**新增：画布讲义编辑器**

- `web/src/editor/` —— 画布上自由排版（题目/标题/知识点/正文/公式/表格/表情/留白），
  多页、可保存再编辑、字号/行距/页面比例（16:9 / A4 / 4:3）/标题与正文字体可调，
  导出 PDF 到 `试卷/`。使用说明见 `设计/讲义系统.md`。
- 后端 `amti/slidev_handout.py` 负责 LaTeX→Slidev 转换与导出；
  前端 `web/src/lib/qlatex.ts` 是它的镜像，**两条路径必须同时改**（有测试比对）。

**结构重构**（审查报告 CPLX-1 / REACT-4）

- `App.tsx` 4,741 → **1,014 行**；`CanvasEditor` 752 → **87 行**（只负责组装）
- 拆出 `useQuestionList` / `useCanvas` / `useDrawer` / `useHandoutIo` 四个 hook，
  以及 `PointDrawer` / `CanvasStage` / `Inspector` 三栏组件
- 拆出页面组件到 `web/src/app/`，共享类型与请求层到 `web/src/lib/`

**引入 git**

- 仓库初始化，代码 + 题库（`题目/*.tex`）+ 配图（`图片/`）入库；
  忽略依赖、构建产物、程序自身的备份、生成的 PDF
- 从此不再用 `.bak` 手工备份；出问题 `git checkout` 回滚

---

## 更早（git 之前）

题库建设、组卷（高考卷/测试题/考点覆盖卷/elegantnote 讲义）、
识图录入、白纸求解、规范审查、四层验收 —— 见 `README.md` 与
`设计/经验教训复盘.md`。git 从 v1.0 开始有记录。
