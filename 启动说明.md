# AmTiKu 怎么启动

## 一句话

```bash
AmTiKu
```

**在任何目录**都能用。浏览器打开 **http://127.0.0.1:8899**

（`AmTiKu` 装在 `~/.local/bin/AmTiKu`，是个指向题库目录的软链接。）

---

## 三样东西

| | 是什么 | 端口 |
|---|---|---|
| **题库服务** | 网页界面；查题、筛题、组卷、导出 | 8899 |
| **本地模型** | LM Studio，求解题目用 | 1234 |
| **求解任务** | 逐题解模拟题，一道一写 | 无 |

`./启动.sh` 会把三样都起起来（**已经在跑的不会重复起**）。

## 常用命令

```bash
AmTiKu           # 启动（默认动作）
AmTiKu 启动      # 同上
AmTiKu 状态      # 看现在跑着什么、解了多少题
AmTiKu 停止      # 停掉题库和求解（LM Studio 不动）
AmTiKu 网页      # 起题库并打开浏览器
AmTiKu 求解      # 只起求解任务
AmTiKu 验收      # 跑四层验收
AmTiKu 去        # 显示题库目录
```

**拼音也认**（省得切输入法）：

```bash
AmTiKu qidong    AmTiKu zhuangtai    AmTiKu tingzhi
AmTiKu wangye    AmTiKu qiujie       AmTiKu yanshou
```

项目里也留了个 `./启动.sh`，功能和 `AmTiKu 启动` 一样。

不想用脚本的话，三条原始命令：

```bash
python3 -m amti.web.server --port 8899     # 题库
lms server start                            # 本地模型（LM Studio）
python3 -m amti.solve                       # 求解
```

## 日志

| 文件 | 内容 |
|---|---|
| `/tmp/amti_web.log` | 题库服务的输出 |
| `/tmp/amti_solve.log` | 求解进度（一道一行） |
| `/tmp/ruku/solve.log` | 求解历史（所有轮次累加） |

---

## 验收

改完数据或程序，跑这一条：

```bash
python3 amti.py accept
```

四层全绿才算过：

```
① 单元自检   8 个模块 + 解析回归 + 检查器自证
② 规范审查   12 项，全库
③ 端到端渲染 用前端同一条渲染路径跑全库（KaTeX / LaTeX 泄漏 / 图片）
④ 快照       内容变化必须是 0
```

## 常见操作

```bash
python3 amti.py status       # 库有多大
python3 amti.py diff         # 跟基线比，内容变化必须是 0
python3 amti.py conform      # 规范审查（只报不改）
python3 amti.py changes      # 变更记录列表
```

---

## 注意

**求解任务在跑的时候，`amti.py diff` 会报「内容变化 N」—— 那是正常的**，
求解本来就在写库。要看存量题有没有被误改，先 `./启动.sh stop` 停掉求解再 diff。

**停 LM Studio**：`lms server stop`。别的程序可能也在用它，所以 `./启动.sh stop` 不碰它。
