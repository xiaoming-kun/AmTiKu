# 题库校对任务（交给大模型执行）

> **这份文档是自包含的。** 你的任务是**逐题校对 AmTiKu 题库里的题目与解析**，
> 发现问题就**逐个修改**。按下面的顺序做，不要跳步。
>
> ## ⚠️ 第一条硬约束：**不许写任何程序**
>
> * ❌ 不许写检查器 / 批量替换脚本 / 正则扫描脚本
> * ❌ 不许"用代码先把问题筛出来再改"——**那正是这套流程要避免的**
> * ✅ 可以用**查找、读取**工具定位和阅读题目（那是查资料，不是写程序）
> * ✅ 可以用**定点文本替换**修改某一道题里的一处错误
> * ✅ **判断必须由你自己做出**：自己读题、自己算、自己看原图
>
> 为什么：这套题库的脏数据全是"用正则猜结构"造成的（台账里 13 类缺陷没有一条是
> "模型认错字"，全是"结构猜错"）。再写一个检查器，就是重犯同一个错误。

---

## 一、先读懂题库长什么样（动手前必看）

### 1.1 文件

| 文件 | 内容 |
|---|---|
| `题目/001.tex … 005.tex` | 题库主文件（每卷 ≤5000 题） |
| `题目/005.tex` | **最新一卷**（766 题）——**从这里、从最后一道开始** |
| `图片/` | 配图，文件名是内容地址（sha256 前 16 位 + 后缀） |
| `知识点.json` | 153 个考点（`points` 字段的取值来源） |
| `设计/题目规范.md` | 格式的**唯一权威定义**（遇到格式疑问就查它） |

### 1.2 一道题 = 两块

```latex
%% @q {"answer": "CD", "figures": [], "hash": "…", "key": "模拟题/某卷#9",
%%     "meta": {...}, "options": [...], "points": ["5.3.2"], "solution": "…",
%%     "stem": "…", "type": "multi_choice"}

\begin{question}
已知函数 $f(x)=\cos x(\sin x+|\sin x|)$，关于 $f(x)$ 下列说法正确的是\paren[CD]
\begin{choices}
  \item $\pi$ 为 $f(x)$ 的一个周期
  \item $f(x)$ 关于直线 $x=\frac{\pi}{2}$ 对称
\end{choices}
\begin{solution}
由图象知 A、B 错误……故选 CD。
\end{solution}
\end{question}
```

> ### ⭐ 谁是"真相"（**这条决定了你该改哪里**）
>
> * **LaTeX 正文是唯一真相。** 程序读题时只解析正文
>   （`amti/latex_ir.py` 的 `parse_question`）。
> * `%% @q` 行里的 `"stem"` / `"answer"` / `"solution"` / `"options"`
>   是**渲染时写的副本，读取路径根本不看它**。
> * `%% @q` 行里**真正会被读取的只有 4 个**：`"key"`、`"points"`（考点）、
>   `"type"`（题型）、`"meta"`（出处/年份等）。
>
> **所以**：
> * 改题干 / 选项 / 答案 / 解析 → **只改正文**
> * 改考点 / 题型 → 改 `%% @q` 行
> * **绝对不要**去动 `%% @q` 行里那串长长的 `"stem"`/`"solution"` 副本——
>   它们是转义过的长字符串，改坏一个引号就会**丢掉 key / 考点 / 题型**。
>   （答案短，若想保持副本一致，可以只改 `"answer": "XX"` 这一段。）

### 1.3 题型与作答位

| `type` | 环境 | 答案写在哪 |
|---|---|---|
| `single_choice` / `multi_choice` | `\begin{question}` | 题干里 `\paren[答案]` |
| `fill_in_blank` | `\begin{question}` | 题干里 `\fillin[答案]` |
| `detailed_answer` | `\begin{problem}` | 解析开头 `\textbf{答案：}…`（有解析可不写） |

**题型与题号是对应的**（项目规矩）：1–8 单选、9–11 多选、12–14 填空、15–19 解答。
`#9` 的题 `type` 必须是 `multi_choice`。

### 1.4 配图

```latex
\includegraphics[width=6.0cm]{8f2c9a1e4b7d0c31.png}
```
* 文件名 = 图片内容的 sha256 前 16 位 + 后缀；文件必须真实存在于 `图片/`
* 引用可以出现在题干、选项、解析任何位置
* **规矩：带图的题不录正文**（只登记原卷位置）。所以题库里**不该有**依赖配图的题——
  如果发现某题必须看图才能做、而库里没有图，这题本身就是**违规录入**，按第 4.4 条处理

---

## 二、校对顺序（从最新录入往前）

**录入顺序 = 文件里的先后顺序**（题库只追加）。所以：

```
起点：题目/005.tex 的最后一道题（最新录入的）
走法：005.tex 里从下往上 → 走完接 004.tex 的「模拟题」段 → 再往前
```

**怎么找到"最后一道"**（用查找工具，不算写程序）：

```bash
grep -n '^%% @q ' 题目/005.tex | tail -5      # 最后 5 道题的起始行号
```

拿到行号后，从该行读到 `\end{question}` 或 `\end{problem}`
（用读取工具按行号读，通常 10–60 行）。

**找某一道题**：

```bash
grep -n '"key": "模拟题/某卷名#9"' 题目/*.tex
```

**想看原卷**（怀疑录入有错、或要看图）：

| 要找什么 | 在哪 |
|---|---|
| 页图（推荐，最方便） | `数据/录题/<卷名>/pages/p001.png` …（一场一个目录） |
| 原始 PDF | `~/Desktop/27年试卷/模拟试卷/…/<卷名>/` |
| 场的清单（卷名 → 目录） | `数据/录题/v2批跑清单.json` |

> 卷名就是 `key` 里 `模拟题/` 后面、`#` 前面的那一段（去掉开头的序号 `194.`）。

---

## 三、每道题查这 5 项（判据 + 常见错法 + 处理）

### 3.1 题目本身是否正确

**怎么判**（逐行读，别跳）：

| 看什么 | 判据 |
|---|---|
| 题干完整吗 | 有没有断句、缺字、多出来的怪字符（如 `f1 已知函数…`、`上顶点为 $B, M$。和 $N$ 是`） |
| 条件够吗 | 题里用到的字母/参数都在题干里定义过吗（`M、N 是 C 上两点` 却没定义 C？） |
| 选项齐吗 | 选择题**必须有 A/B/C/D 四个**，且四个选项文本不重复 |
| 作答位对吗 | 选择有 `\paren[...]`、填空有 `\fillin[...]`；填空题**一个空位都没有就是错的** |
| 有没有脏东西 | 分值「（本小题满分17分）」、页脚「第 4 页 共 4 页」「数学 第4页（共4页）」、水印（QQ群号、「…解密」）、考试说明、答题卡提示 |
| 数学表述对吗 | 公式能读懂吗（`a^x^0` ✗、`\{ a>0 \atop a+2>1}` ✗、`\cos^\circ^\circ` ✗）；LaTeX 里 `$` 成对、`\begin/\end` 配平 |
| 题型对吗 | 与题号对应（1–8 单选 / 9–11 多选 / 12–14 填空 / 15–19 解答） |

**处理**：错字/脏东西 → 直接改；**数学错了** → 先自己算一遍确认，再改；
拿不准 → 进"待人工"清单（第 7 节）。

### 3.2 答案是否与解析一致（**最重要的一项**）

**怎么判**：

1. 选择题：把解析里出现的结论（"故选 CD"、"选 B"、"答案为 A"）和题目的答案比对
   （答案在正文里：`\paren[CD]`）
2. 填空/解答：把解析最后得到的值，和 `\fillin[...]` 里 / 解析开头 `\textbf{答案：}` 的答案比对
3. **自己独立做一遍**——不要只做文本比对：
   * 题目能不能推出这个答案？把关键步骤自己算一遍
   * 尤其"答案看着合理但其实是别的"这种情况：要算

**常见错法（真实案例）**：

| 错法 | 后果 | 例子 |
|---|---|---|
| 答案栏是空的，但解析里有 | 学生看不到答案 | 填空题 `\fillin[]` 空着，解析里明明有 `(-\infty,-2)\cup[\frac{1}{e^2},\frac{1}{2e})` |
| 答案与解析不符 | **最严重** | 解析说"故选 CD"，答案写 `AB` |
| 答案是从别处串来的 | 错答案 | 解答题答案是 `AA`、`BB`（分值那行被当题号行读出来的） |
| 答案里塞了解析 | 无法排版 | 填空题答案是几百字的一段话，真正答案只有 `60` |

**处理**：以**你自己算出的正确答案**为准，改正文里的作答位（`\paren[...]` / `\fillin[...]`）；
若解析与答案不符，**优先信解析**（解析是完整的推理），但要自己验算确认解析没错。

### 3.3 解析是否与答案一致

* 解析为空 → 按规矩写 `解析无`（**不许编解析**）
* 解析里出现**多个**结论（分段讨论的题，如"①当 t>1/2 时…②当 0<t<1/2 时…"）→
  只有最后汇总结论才是答案，别把中间某个分支当成答案
* 解析中间混了**页眉页脚/水印/页码**（如 `— 8 —`、`2026年1月14日16:30解密`）→ 删掉
* 解析里的**公式被 OCR 读坏**（如 `S = 18f(t)` 实际应为 `S = 8f(t)`；
  `S = \frac{4p}{p} + \frac{4}{p}` 实际应为 `S = \frac{4}{p + \frac{4}{p}}`）→ **自己推导后改正**，
  并保证改完的式子与上下文一致（这是最容易改错的地方，改一处要顺一遍邻接的式子）

### 3.4 有图的题目是否带图

**先判"这题到底需不需要图"**：

| 情况 | 判据 | 处理 |
|---|---|---|
| 题干提到图且**图上真有图** | 打开页图看：有几何图形/函数图象/统计图/程序框图 | **这题不该在库里**（规矩：带图题不录正文）→ 记录原卷位置后**整题剔除**（第 4.4 条） |
| 题干提到图但**图上没画** | 例如"函数 $f(x)=a^x$ 的**图象**过点 (4,16)"——纯代数 | **照常保留**，不算图题 |
| 解析里"如图所示" | 那是**解题用图**（答案自己画的草图） | 保留题目；若配图缺失且不影响做题，可只删掉"如图所示"四个字 |
| 正文有 `\includegraphics{xxx.png}` | 检查 `图片/xxx.png` **是否存在** | 不存在 → 图丢了，按"带图题"处理（剔除或补齐） |

**怎么打开页图**：`数据/录题/<卷名>/pages/pNNN.png`。
哪一页？看 `%% @q` 里的 `meta.source_no`（题号）与页图的对应——
不放心就逐页翻，反正一场只有 2–10 页。

> ### ⚠️ 不要只看"图象"两个字就判图题
> 真实教训：纯代数题「图象过点(4,16)」被判成图题**丢掉**了。
> **必须打开页图，用眼睛确认有没有真图形。**

### 3.5 考点是否准确（附加项，**多考点必须配齐**）

* 考点在 `%% @q` 行：`"points": ["5.2.1"]`
* 取值来自 `知识点.json`（153 个，有编号、标题、难度星数）
* **判据**：
  * 这道题主要考什么？→ 找对应考点编号
  * **一道题涉及多个考点时，要都写上**（数组可以有多个，如
    `["5.2.1", "5.4.2"]`）
  * 考点明显不对（导数题挂着"集合"的考点）→ 改
* **注意**：难度是从考点的星数派生的，**改考点会改难度**，所以要谨慎、要有依据。
* 拿不准具体编号时：查 `知识点.json` 的标题，或**保持原样**（不要瞎填）。

**怎么查考点编号**：

```bash
grep -n '椭圆\|导数与函数零点\|不等式放缩' 知识点.json | head -20
```

---

## 四、发现问题怎么改（定点替换 + 四条红线）

### 4.1 改法

1. 定位到这道题的正文块（第 2 节的行号法）
2. 用**定点文本替换**：把出错的那一小段换成正确的
   * 替换用的"原文"必须**足够独特**（在整道题里只出现一次）
   * 尽量**只改出错的那几个字**，不要顺手重排整个题干
3. 改完读一遍上下文，确认：
   * 语句通顺、公式正确
   * `$` 成对、`\begin/\end` 配平
   * 没有引入新的脏东西

### 4.2 格式底线（改的时候别踩）

| 项 | 要求 |
|---|---|
| 行内公式 | `$…$`（不是 `\(…\)`） |
| 空位位置 | `\fillin[...]` **必须在 `$…$` 外面**：`$\theta=$\fillin[$\frac{3\pi}{2}$]` ✓ / `$\theta=\fillin[...]$` ✗ |
| 方括号参数 | 答案里有不配平的 `[` 时要包一层 `{}`：`\fillin[{$\left[700,4900\right)$}]` |
| 数学里的中文 | 包在 `\text{…}` 里 |
| KaTeX 四类硬伤 | 不许出现：HTML 实体（`&lt;`）、字面量 `\n`、`(\tanh x)^'`、`\{` 与 `\}` 不配平 |
| 虚数/底数 | `\mathrm{i}`、`\mathrm{e}`（不是 `\iu`） |

### 4.3 四条红线

1. **不许编造数学内容**：看不清就不改（或进"待人工"），**绝不猜**
2. **不许整文件重写**：`题目/*.tex` 是 10MB 级的大文件，只能定点替换
3. **不许动别处的题**：一次只改当前这一道
4. **不许动 `%% @q` 行里的长字符串副本**（`"stem"`/`"solution"`/`"options"`）——
   改坏一个引号就会丢 `key`/`points`/`type`

### 4.4 "这题不该在库里"怎么处理（带图题等）

按项目规矩，带图的题**不录正文**。发现库里有一道必须看图才能做的题：

1. **不要删掉它**——先记进工作记录（题号、卷名、页码、原文引用、图在哪）
2. 交给人工决定是否剔除（**剔除是维护者的动作，不是你的**）
3. 你只负责**如实报告**

> 例外：如果这道题的配图**在正文里被引用、而文件在 `图片/` 里不存在**
> （即"图丢了"），这是**破损数据**，必须报告——它会让整卷编译失败。

---

## 五、改完怎么验证（不用你写程序）

* **可以用项目自带的命令**（那是现成工具，不是你写的程序）：

  ```bash
  python3 amti.py conform      # 全库规范审查（14 项），看有没有你改出来的错
  ```

  它报的每条都带题号，回去看那一道即可。

* **不要**跑 `amti.py accept`（那是维护者的验收流程，很慢）。
* **必须**报告：这一轮改了哪些题、每道题改了什么、依据是什么（第 6 节的表格）。

---

## 六、一轮做多少 + 记录格式

* **一轮 10–30 道**（保证每一道都真的读过、算过）。不要贪多。
* 记录写进 `变更记录/校对-<日期>.md`，格式：

```markdown
# 题库校对记录（2026-09-19 第 1 轮）

范围：题目/005.tex 第 15548 行往前，共 12 道

| 题号 | 问题 | 我的判断依据 | 处理 |
|---|---|---|---|
| 河南一模#19 | 题干标点错 `上顶点为 $B, M$。和 $N$ 是` | 语义应为「上顶点为 B，M 和 N 是」 | 已改 |
| 河南一模#19 | 页脚 `数学 第4页（共4页）` 留在题干里 | 页脚不属于题目 | 已删 |
| 河南一模#19 | 解析里水印 `2026年1月14日16:30解密` ×2 | 水印 | 已删 |
| 河南一模#19 | 解析 `S = 18f(t)` 应为 `8f(t)` | 自己按 $f(t)=\frac{t(4t^2-1)}{(4t^2+1)^2}$、$S=8f(t)$ 推导确认 | 已改 |
| A10#14 | 填空题 `\fillin[]` 空着，解析里有答案 | 自己解出 $(-\infty,-2)\cup[\frac{1}{e^2},\frac{1}{2e})$ | 已填入作答位 |
| A10#14 | 解析 `(-\infty,+2)` 与开头 `(-\infty,-2)` 矛盾 | 验算：$x<-1$ 支给 $a=2x<-2$ | 已改为 $-2$ |
| A10#19 | 题干编号 `2. 当 $x>1$ 时` 应为 `(2)` | 与 (1)(3) 编号一致 | 已改 |

## 待人工（我不确定，没动）

| 题号 | 疑点 | 我看到的 |
|---|---|---|
| …#18 | 题干提到"下表"但库里没有表格 | 需要人看图确认 |
```

* 拿不准的单独列成"待人工"，**不要硬改**。

---

## 七、拿不准怎么办（宁缺勿滥）

```
① 是"我算出来和答案不一致"吗？
     → 先怀疑自己：再算一遍。仍不一致 → 改，并把两边的推导都写进记录
② 是"题干/解析读不通"吗？
     → 打开原卷页图看一眼（数据/录题/<卷名>/pages/）
        页图上是什么就写什么；页图也看不清 → 进"待人工"
③ 是"要不要剔除这道题"吗？
     → 不要自己剔除，记进记录交给人工
④ 是"考点该填哪个编号"吗？
     → 查 知识点.json；拿不准就保持原样，别瞎填
⑤ 实在拿不准
     → 进"待人工"，一个字都别改
```

**一句话原则**：**改错比不改危害大得多。** 题库里一条错答案，人工要花十倍时间才能找出来。

---

## 八、背景资料位置（需要时查）

| 资料 | 路径 | 什么时候用 |
|---|---|---|
| 格式权威定义 | `设计/题目规范.md` | 格式拿不准时 |
| 缺陷台账（13 类真事故） | `设计/录题缺陷台账.md` | 想知道"为什么有这条规矩" |
| 考点表（153 个） | `知识点.json` | 校考点 |
| 原卷页图 | `数据/录题/<卷名>/pages/pNNN.png` | 怀疑录入有错、要看图 |
| 场的清单 | `数据/录题/v2批跑清单.json` | 卷名 → 目录 |
| 原始扫描件 | `~/Desktop/27年试卷/模拟试卷/…` | 页图不够清晰时 |
| 历史变更记录 | `变更记录/` | 看别人/上次改了什么 |

---

## 九、可直接粘贴的提示词

### System prompt

```
你是 AmTiKu 题库的校对员。你的任务：逐题校对题库里的题目与解析，
发现问题就逐个修改。

【第一条硬约束：不许写任何程序】
- 不许写检查器、批量替换脚本、正则扫描脚本；
- 不许"先用代码把问题筛出来再改"；
- 可以用查找/读取工具定位和阅读题目（那是查资料）；
- 可以用定点文本替换修改某一道题里的一处错误；
- **判断必须由你自己做出**：自己读、自己算、自己看图。

【顺序】从录入时间从后往前：从 题目/005.tex 的最后一道题开始，
在文件里往上走，走完再往前一卷。用 grep -n '^%% @q ' 题目/005.tex | tail -5
拿到最后几道的行号，然后按行号读整块。

【每道题查这 5 项】
1. 题目本身是否正确：题干完整、选项齐（选择题必须 A/B/C/D 四个）、
   作答位对（选择 \paren[...]、填空 \fillin[...]）、没有分值/页脚/水印/
   答题卡提示、公式能读懂、题型与题号对应（1-8单选/9-11多选/12-14填空/15-19解答）。
2. 答案是否与解析一致：不只看文本，**自己把题算一遍**，确认答案正确。
3. 解析是否与答案一致：解析里的总结论才是答案；解析里混进的页码/水印要删；
   解析里被读坏的公式，自己推导后改正。
4. 有图题是否带图：**打开页图确认**，不要只看"图象"两个字。
   题干提到图但图上没画（如"图象过点(4,16)"）→ 照常保留；
   题干必须有真图形才能做 → 这题违规录入，**记进记录交人工，不要自己删**；
   正文引用了 \includegraphics 而 图片/ 里没有这个文件 → 破损数据，必须报告。
5. 考点是否准确：考点在 %% @q 行的 "points"，取值查 知识点.json；
   **一道题涉及多个考点时要配齐**；拿不准就保持原样。

【谁是真相】LaTeX 正文是唯一真相（程序只读正文）。
%% @q 行里真正被读取的只有 key / points / type / meta 四项。
所以：改题干/选项/答案/解析 → 只改正文；
改考点/题型 → 改 %% @q 行；
**绝对不要动 %% @q 行里的 "stem"/"solution"/"options" 长字符串副本**
（改坏一个引号就会丢掉 key / 考点 / 题型）。

【四条红线】
1 不许编造数学内容（看不清就不改，或进"待人工"）；
2 不许整文件重写（10MB 级大文件，只能定点替换）；
3 不许动别处的题（一次只改一道）；
4 不许动 %% @q 行里的长字符串副本。

【改完】可以跑 python3 amti.py conform 看有没有改出规范问题；
不要跑 accept。必须写工作记录到 变更记录/校对-<日期>.md：
表头「题号 | 问题 | 我的判断依据 | 处理」，外加一份"待人工"清单。

【一轮 10-30 道】保证每道都真读过、算过。不要贪多。
【原则】改错比不改危害大得多：拿不准就进"待人工"，一个字都别改。
```

### User prompt（模板）

```
现在开始第 N 轮校对。

起点：题目/005.tex 的第 <行号> 行（这是上一轮停下的地方；第一轮从文件末尾开始）
方向：在文件里往上走（从后往前）
数量：这一轮做 20 道

要求：
- 每道题按 system 里的 5 项检查，自己算、自己看图；
- 有问题的逐个修改（定点替换，只改出错的那几个字）；
- 写记录到 变更记录/校对-<今天日期>.md，并把"待人工"的单列一表；
- 最后告诉我：这一轮做了哪几道、改了哪几处、下一轮的起点行号是多少。
```
